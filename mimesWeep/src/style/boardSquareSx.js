import { blue, green, pink, red, deepPurple, purple, blueGrey, teal } from '@mui/material/colors';

const squareSize = '36px';

export const squareSx = {
    maxWidth: squareSize,
    maxHeight: squareSize,
    minWidth: squareSize,
    minHeight: squareSize
};

export const iconSize = '24px';

export const one = { color: blue[500] };

export const two = { color: green[500] };

export const three = { color: pink[300] };

export const four = { color: purple[300] };

export const five = { color: teal[500] };

export const six = { color: deepPurple[500] };

export const seven = { color: blueGrey[500] };

export const eight = { color: red[500] };